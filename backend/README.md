# Backend (Phase 2 — not yet built)

This folder is a placeholder for the future backend: real bookings, auth,
and payments. See `../docs/PROJECT_PLAN.md` and `../docs/BOOKING_SPEC.md`
for the plan.

Likely stack: Node/Express or a BaaS (Supabase/Firebase), Stripe for
payments. The frontend's mock data layer (`frontend/src/data/tours.js`)
and Context state are already shaped to make this swap straightforward.
