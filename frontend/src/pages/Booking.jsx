import "./Placeholder.css";

// Full 3-step flow (traveler details -> review -> confirmation) arrives
// in Phase 4 per docs/BOOKING_SPEC.md.
export default function Booking() {
  return (
    <div className="container placeholder">
      <span className="placeholder__tag">Phase 4 will build this out</span>
      <h1>Booking</h1>
      <p>The traveler details → review → confirmation flow arrives in Phase 4.</p>
    </div>
  );
}
